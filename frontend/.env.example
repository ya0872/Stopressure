# バックエンド（FastAPI）のベースURL。frontend/.env にコピーして使う。
#
# 127.0.0.1 を使うこと。Windows のブラウザは localhost を先に ::1（IPv6）へ解決するが、
# uvicorn は --host 127.0.0.1 で待ち受けているため ::1 側は接続拒否になる（CLAUDE.md の注記）。
VITE_API_BASE=http://127.0.0.1:8000/api/v1
